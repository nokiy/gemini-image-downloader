# Folder: src/background

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Service worker for tracking downloads and renaming files / 跟踪下载和重命名文件的服务工作器
Listens to download events and manages pending rename operations / 监听下载事件并管理待处理的重命名操作
Uses Chrome storage session/local to persist download tracking state / 使用 Chrome 存储会话/本地来持久化下载跟踪状态

## Files
- `service_worker.js`: Service worker handling download tracking and rename / 处理下载跟踪和重命名的服务工作器
