# Folder: src/utils

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Shared utility modules used across the extension / 扩展中共享的工具模块
Provides unified logging API with error-logger integration / 提供与错误日志集成的统一日志 API
Cross-module utilities with no circular dependencies / 无循环依赖的跨模块工具

## Files
- `logger.js`: Unified logging layer with debug/info/warn/error levels / 带 debug/info/warn/error 级别的统一日志层
